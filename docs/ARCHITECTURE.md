# Architecture

The intended access model is class-based: each student belongs to a class and has a unique student access code. Classes can be granted courses and classrooms. This document is a design checkpoint; implementation must reuse existing authentication and database conventions.