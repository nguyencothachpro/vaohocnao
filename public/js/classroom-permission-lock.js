(()=>{
const C=window.CLASSROOM||{},$=id=>document.getElementById(id),socket=window.io?window.io():null;
let canNavigate=!!C.isTeacher||!!C.canNavigate||C.roomStatus!=='live';
let canWrite=!!C.isTeacher||!!C.canWrite; let roomStatus=C.roomStatus||'live';
const locked=()=>!C.isTeacher&&roomStatus==='live'&&!canNavigate;
const toast=m=>{const e=$('c3Toast');if(!e)return;e.textContent=m;e.classList.add('show');clearTimeout(e._t);e._t=setTimeout(()=>e.classList.remove('show'),1600)};
function applyLock(){const l=locked();['c3Prev','c3Next','c3PageInput'].forEach(id=>{const e=$(id);if(!e)return;e.disabled=l;e.style.opacity=l?'.5':'1'});document.querySelectorAll('.c3-tool[data-mode="pen"],.c3-tool[data-mode="highlighter"],.c3-tool[data-mode="eraser"],#c3Clear').forEach(e=>{e.disabled=!canWrite;e.style.opacity=canWrite?'1':'.45'});const req=$('c3RequestWrite');if(req)req.style.display=canWrite?'none':'';const view=$('c3View');if(view)view.classList.toggle('c3-student-locked',l);let badge=document.getElementById('c3PermissionBadge');if(!badge&&!C.isTeacher){badge=document.createElement('div');badge.id='c3PermissionBadge';badge.className='c3-permission-badge';document.body.appendChild(badge)}if(badge){badge.textContent=l?'👁 Đang theo dõi giáo viên — trang bị khóa':'✓ Bạn được phép mở trang';badge.classList.toggle('locked',l)}}
function injectStyle(){if(document.getElementById('c3PermissionStyle'))return;const s=document.createElement('style');s.id='c3PermissionStyle';s.textContent=`.c3-student-locked .c3-book{pointer-events:none!important}.c3-permission-badge{position:fixed;right:18px;bottom:18px;z-index:99999;padding:7px 11px;border-radius:999px;background:#fff;border:1px solid #dbe3ef;box-shadow:0 4px 18px rgba(15,23,42,.12);font-size:12px;color:#166534}.c3-permission-badge.locked{color:#475569}.c3-permission-tools{margin:10px 0;padding:10px;border:1px solid #dbe3ef;border-radius:10px;background:#fff}.c3-permission-student{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:6px;align-items:center;padding:7px 0;border-top:1px solid #eef2f7;font-size:12px}.c3-permission-actions{display:flex;gap:5px}.c3-permission-student button{font-size:11px;padding:4px 7px;border:1px solid #cbd5e1;border-radius:6px;background:#fff}.c3-permission-student button.active{background:#dcfce7;border-color:#86efac;color:#166534}.c3-permission-note{font-size:11px;color:#64748b;margin-top:5px}`;document.head.appendChild(s)}
async function getPerm(){try{const r=await fetch('/phong-hoc/'+encodeURIComponent(C.room)+'/permissions',{credentials:'same-origin',cache:'no-store'});if(!r.ok)return null;return await r.json()}catch(_){return null}}
async function studentPoll(){if(C.isTeacher)return;const p=await getPerm();if(!p)return;roomStatus=p.roomStatus||roomStatus;canNavigate=!!p.canNavigate||roomStatus!=='live';canWrite=!!p.canWrite;applyLock()}
async function setPerm(id,field,value){try{const r=await fetch('/phong-hoc/'+encodeURIComponent(C.room)+'/permissions/'+id,{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({[field]:!!value})});if(!r.ok)throw new Error('Không cập nhật được quyền');if(field==='canWrite')socket?.emit('classroom:write-grant',{userId:id,allow:!!value});return await r.json()}catch(e){toast(e.message||'Lỗi cập nhật quyền')}}
async function teacherRoster(){if(!C.isTeacher)return;const p=await getPerm();if(!p?.students)return;let box=document.getElementById('c3PermissionTools');if(!box){const panel=document.getElementById('panel-materials');if(!panel)return;box=document.createElement('div');box.id='c3PermissionTools';box.className='c3-permission-tools';panel.insertBefore(box,panel.firstChild?.nextSibling||panel.firstChild)}box.innerHTML='<div class="c3-heading">Quyền học viên</div><div class="c3-muted">Cấp riêng <b>Viết</b> hoặc <b>Mở trang</b> cho từng học viên.</div><div class="c3-permission-note">Đang dạy: học viên không có quyền Mở trang chỉ xem theo giáo viên. Đóng lớp: học viên được tự lật trang nhưng vẫn không được viết nếu chưa được cấp quyền.</div>';p.students.forEach(st=>{const row=document.createElement('div');row.className='c3-permission-student';const n=document.createElement('span');n.textContent=(st.display_name||'Học viên')+' · '+st.student_code;const a=document.createElement('div');a.className='c3-permission-actions';const w=document.createElement('button');w.textContent=st.can_write?'✓ Viết':'Viết';w.classList.toggle('active',st.can_write);w.onclick=async()=>{await setPerm(st.id,'canWrite',!st.can_write);teacherRoster()};const v=document.createElement('button');v.textContent=st.can_navigate?'✓ Mở trang':'Mở trang';v.classList.toggle('active',st.can_navigate);v.onclick=async()=>{await setPerm(st.id,'canNavigate',!st.can_navigate);teacherRoster()};a.append(w,v);row.append(n,a);box.appendChild(row)})}
function install(){injectStyle();applyLock();if(C.isTeacher){teacherRoster();setInterval(teacherRoster,3000)}else{studentPoll();setInterval(studentPoll,1200)}socket?.on('classroom:write-status',m=>{if(C.memberId&&Number(m?.userId)===Number(C.memberId)){canWrite=!!m.allow;applyLock()}});socket?.on('classroom:navigate-status',m=>{if(C.memberId&&Number(m?.userId)===Number(C.memberId)){canNavigate=!!m.allow;applyLock()}})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(install,800));else setTimeout(install,800);
})();
