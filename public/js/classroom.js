(() => {
  const C=window.CLASSROOM||{};const socket=io();const pagesEl=document.getElementById('pages');const viewport=document.getElementById('bookViewport');const emptyBoard=document.getElementById('emptyBoard');const zoomLabel=document.getElementById('zoomLabel');const pageNumber=document.getElementById('pageNumber');const pageTotal=document.getElementById('pageTotal');let mode='pen',currentPage=0,pages=[],pageFlip=null,pdf=null,zoom=1;let localStream=null,processedStream=null,keyOn=false,keyTimer=null;const peers=new Map();
  function setMode(next){mode=next;document.querySelectorAll('.side-tool').forEach(b=>b.classList.remove('active'));const map={pen:'pen',highlighter:'highlighter',eraser:'eraser',text:'textTool',note:'noteTool'};if(map[next])document.getElementById(map[next])?.classList.add('active')}['pen','highlighter','eraser'].forEach(id=>document.getElementById(id)?.addEventListener('click',()=>setMode(id)));document.getElementById('textTool')?.addEventListener('click',()=>setMode('text'));document.getElementById('noteTool')?.addEventListener('click',()=>document.querySelector('[data-panel="notes"]')?.click());
  function updateZoom(){if(zoomLabel)zoomLabel.textContent=Math.round(zoom*100)+'%';if(pagesEl)pagesEl.style.transform=`scale(${zoom})`}document.getElementById('zoomIn')?.addEventListener('click',()=>{zoom=Math.min(2.5,zoom+.1);updateZoom()});document.getElementById('zoomOut')?.addEventListener('click',()=>{zoom=Math.max(.5,zoom-.1);updateZoom()});document.getElementById('fit')?.addEventListener('click',()=>{zoom=1;updateZoom();viewport?.scrollTo({top:0,left:0,behavior:'smooth'})});document.getElementById('resetView')?.addEventListener('click',()=>{zoom=1;updateZoom();viewport?.scrollTo({top:0,left:0,behavior:'smooth'})});document.getElementById('fullscreen')?.addEventListener('click',async()=>{const target=document.querySelector('.classroom-app');if(!document.fullscreenElement)await target?.requestFullscreen?.();else await document.exitFullscreen?.()});
  function emitBoard(payload){if(C.isTeacher||C.canWrite)socket.emit('classroom:board',{...payload,user:C.currentUser||(C.isTeacher?'Giáo viên':'Học viên')})}
  function makePage(index,pdfCanvas){const host=document.createElement('div');host.className='page-host';host.dataset.page=index;host.style.aspectRatio='0.707 / 1';if(pdfCanvas){pdfCanvas.style.width='100%';pdfCanvas.style.height='100%';host.appendChild(pdfCanvas)}const draw=document.createElement('canvas');draw.className='draw-layer';host.appendChild(draw);const resize=()=>{const r=host.getBoundingClientRect();if(!r.width||!r.height)return;const old=draw.width&&draw.height?draw.toDataURL():null;draw.width=Math.round(r.width*devicePixelRatio);draw.height=Math.round(r.height*devicePixelRatio);draw.style.width=r.width+'px';draw.style.height=r.height+'px';const ctx=draw.getContext('2d');ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);if(old){const img=new Image();img.onload=()=>ctx.drawImage(img,0,0,r.width,r.height);img.src=old}};if(window.ResizeObserver)new ResizeObserver(resize).observe(host);requestAnimationFrame(resize);let drawing=false,last=null;draw.addEventListener('pointerdown',e=>{if(!(C.isTeacher||C.canWrite))return;if(mode==='text'){const text=prompt('Nhập nội dung cần viết:');if(!text)return;const ctx=draw.getContext('2d');ctx.save();ctx.globalCompositeOperation='source-over';ctx.fillStyle='#111827';ctx.font='20px Arial';ctx.fillText(text,e.offsetX,e.offsetY);ctx.restore();emitBoard({page:index,kind:'text',x:e.offsetX,y:e.offsetY,text});return}draw.setPointerCapture(e.pointerId);drawing=true;last=[e.offsetX,e.offsetY]});draw.addEventListener('pointerup',()=>{drawing=false;last=null});draw.addEventListener('pointercancel',()=>{drawing=false;last=null});draw.addEventListener('pointermove',e=>{if(!drawing)return;const x=e.offsetX,y=e.offsetY,ctx=draw.getContext('2d'),erase=mode==='eraser';ctx.save();ctx.globalCompositeOperation=erase?'destination-out':'source-over';ctx.lineWidth=erase?24:(mode==='highlighter'?18:3);ctx.lineCap='round';ctx.lineJoin='round';ctx.strokeStyle=mode==='highlighter'?'rgba(250,204,21,.35)':'#ef4444';ctx.beginPath();ctx.moveTo(last[0],last[1]);ctx.lineTo(x,y);ctx.stroke();ctx.restore();emitBoard({page:index,kind:'stroke',x1:last[0],y1:last[1],x2:x,y2:y,erase,highlight:mode==='highlighter'});last=[x,y]});return host}
  function applyRemote(p){const host=pages[p.page];if(!host)return;const draw=host.querySelector('.draw-layer');if(!draw)return;const ctx=draw.getContext('2d'),r=host.getBoundingClientRect(),sx=r.width/(draw.width/devicePixelRatio),sy=r.height/(draw.height/devicePixelRatio);ctx.save();ctx.globalCompositeOperation=p.erase?'destination-out':'source-over';ctx.lineCap='round';ctx.lineJoin='round';ctx.lineWidth=p.erase?24:(p.highlight?18:3);ctx.strokeStyle=p.highlight?'rgba(250,204,21,.35)':'#ef4444';if(p.kind==='stroke'){ctx.beginPath();ctx.moveTo(p.x1/sx,p.y1/sy);ctx.lineTo(p.x2/sx,p.y2/sy);ctx.stroke()}if(p.kind==='text'){ctx.globalCompositeOperation='source-over';ctx.fillStyle='#111827';ctx.font='20px Arial';ctx.fillText(p.text,p.x/sx,p.y/sy)}ctx.restore()}socket.on('classroom:board',applyRemote);socket.on('classroom:clear',()=>pages.forEach(p=>{const d=p.querySelector('.draw-layer');d?.getContext('2d').clearRect(0,0,d.width,d.height)}));
  function goToPage(n,broadcast=true){n=Math.max(0,Math.min((pages.length||1)-1,Number(n)||0));currentPage=n;if(pageFlip)try{pageFlip.flip(n)}catch(e){}if(pageNumber)pageNumber.value=n+1;if(broadcast&&C.isTeacher)socket.emit('classroom:page',{page:n})}document.getElementById('prev')?.addEventListener('click',()=>goToPage(currentPage-1));document.getElementById('next')?.addEventListener('click',()=>goToPage(currentPage+1));pageNumber?.addEventListener('change',()=>goToPage(Number(pageNumber.value)-1));socket.on('classroom:page',({page})=>{if(!C.isTeacher)goToPage(page,false)});document.addEventListener('keydown',e=>{if(e.target.matches('input,textarea'))return;if(e.key==='ArrowLeft')goToPage(currentPage-1);if(e.key==='ArrowRight'||e.key===' ')goToPage(currentPage+1)});viewport?.addEventListener('click',e=>{if(e.target.closest('button,input,a,.draw-layer'))return;const r=viewport.getBoundingClientRect();if(e.clientX-r.left<r.width*.28)goToPage(currentPage-1);else if(e.clientX-r.left>r.width*.72)goToPage(currentPage+1)});
  async function loadPdf(url){pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';pdf=await pdfjsLib.getDocument(url).promise;pagesEl.innerHTML='';pages=[];for(let i=1;i<=pdf.numPages;i++){const p=await pdf.getPage(i),vp=p.getViewport({scale:1.6}),c=document.createElement('canvas');c.width=vp.width;c.height=vp.height;await p.render({canvasContext:c.getContext('2d'),viewport:vp}).promise;pagesEl.appendChild(makePage(i-1,c))}pages=[...pagesEl.children];pageTotal.textContent=pages.length;emptyBoard?.classList.add('d-none');const w=Math.max(320,Math.min(1000,Math.floor((viewport?.clientWidth||900)*.88)));const h=Math.round(w/0.707);if(window.St?.PageFlip){pageFlip=new St.PageFlip(pagesEl,{width:w,height:h,size:'stretch',minWidth:280,maxWidth:1000,minHeight:396,maxHeight:1420,showCover:true,maxShadowOpacity:.25,usePortrait:true,swipeDistance:15});pageFlip.loadFromHTML(pages);pageFlip.on('flip',e=>{currentPage=e.data;pageNumber.value=currentPage+1;if(C.isTeacher)socket.emit('classroom:page',{page:currentPage})})}goToPage(0,false);updateZoom()}
  async function openMaterial(url,kind,broadcast=true){if(!url)return;if(kind==='pdf'||/\.pdf(?:\?|$)/i.test(url)){try{await loadPdf(url);if(broadcast&&C.isTeacher)socket.emit('classroom:material-open',{url,kind});return}catch(e){alert('Không mở được PDF này. Hãy kiểm tra link chia sẻ.');return}}if(kind==='image'||/\.(png|jpe?g|webp|gif)(?:\?|$)/i.test(url)){const img=new Image();img.crossOrigin='anonymous';img.onload=()=>{const c=document.createElement('canvas');c.width=img.naturalWidth;c.height=img.naturalHeight;c.getContext('2d').drawImage(img,0,0);pagesEl.innerHTML='';pages=[makePage(0,c)];pagesEl.appendChild(pages[0]);pageTotal.textContent='1';emptyBoard?.classList.add('d-none');if(C.isTeacher&&broadcast)socket.emit('classroom:material-open',{url,kind})};img.onerror=()=>alert('Không mở được ảnh tài liệu.');img.src=url;return}window.open(url,'_blank','noopener')}
  document.querySelectorAll('[data-open-pdf]').forEach(b=>b.addEventListener('click',()=>openMaterial(b.dataset.openPdf,'pdf')));document.querySelectorAll('[data-material-url]').forEach(b=>b.addEventListener('click',()=>openMaterial(b.dataset.materialUrl,b.dataset.materialKind)));document.getElementById('openPdf')?.addEventListener('click',()=>{if(document.querySelector('[data-open-pdf]'))openMaterial(document.querySelector('[data-open-pdf]').dataset.openPdf,'pdf')});socket.on('classroom:material-open',({url,kind})=>{if(!C.isTeacher)openMaterial(url,kind,false)});
  async function camera(){try{localStream=await navigator.mediaDevices.getUserMedia({video:true,audio:true});const v=document.getElementById('localVideo');v.srcObject=localStream;document.getElementById('videoPip')?.classList.remove('d-none');if(C.isTeacher)socket.emit('classroom:teacher-stream')}catch(e){alert('Không mở được camera/micro. Hãy cấp quyền camera và micro cho website rồi thử lại.')}}document.getElementById('cam')?.addEventListener('click',camera);document.getElementById('screen')?.addEventListener('click',async()=>{if(!C.isTeacher){alert('Chỉ giáo viên mới chia sẻ màn hình cho cả phòng.');return}try{const s=await navigator.mediaDevices.getDisplayMedia({video:true,audio:true});localStream=s;document.getElementById('localVideo').srcObject=s;document.getElementById('videoPip')?.classList.remove('d-none');socket.emit('classroom:teacher-stream')}catch(e){}});document.getElementById('green')?.addEventListener('click',()=>{if(!C.isTeacher){alert('Tính năng xóa nền xanh chỉ dành cho giáo viên.');return}if(!localStream){alert('Hãy bật Camera trước.');return}keyOn=!keyOn;if(keyOn)applyGreenKey();else{if(keyTimer)cancelAnimationFrame(keyTimer);processedStream=null;document.getElementById('localVideo').srcObject=localStream}});
  function applyGreenKey(){const v=document.getElementById('localVideo'),c=document.getElementById('keyCanvas'),ctx=c.getContext('2d');c.width=640;c.height=360;const tick=()=>{if(!keyOn)return;ctx.drawImage(v,0,0,c.width,c.height);const img=ctx.getImageData(0,0,c.width,c.height),d=img.data;for(let i=0;i<d.length;i+=4){const r=d[i],g=d[i+1],b=d[i+2];if(g>r*1.35&&g>b*1.15&&g>70)d[i+3]=0}ctx.putImageData(img,0,0);keyTimer=requestAnimationFrame(tick)};tick();processedStream=c.captureStream(30);localStream.getAudioTracks().forEach(t=>processedStream.addTrack(t));v.srcObject=processedStream;socket.emit('classroom:teacher-stream')}
  socket.on('classroom:request-stream',async({id})=>{if(!C.isTeacher||!(processedStream||localStream))return;const stream=processedStream||localStream,pc=new RTCPeerConnection();peers.set(id,pc);stream.getTracks().forEach(t=>pc.addTrack(t,stream));pc.onicecandidate=e=>e.candidate&&socket.emit('webrtc:ice',{to:id,candidate:e.candidate});const offer=await pc.createOffer();await pc.setLocalDescription(offer);socket.emit('webrtc:offer',{to:id,offer})});socket.on('classroom:teacher-stream',()=>{if(!C.isTeacher)socket.emit('classroom:request-stream',{id:socket.id})});socket.on('webrtc:offer',async({from,offer})=>{if(C.isTeacher)return;const pc=new RTCPeerConnection();peers.set(from,pc);pc.ontrack=e=>{const v=document.getElementById('remoteVideo');v.srcObject=e.streams[0];document.getElementById('remotePip')?.classList.remove('d-none')};pc.onicecandidate=e=>e.candidate&&socket.emit('webrtc:ice',{to:from,candidate:e.candidate});await pc.setRemoteDescription(offer);const answer=await pc.createAnswer();await pc.setLocalDescription(answer);socket.emit('webrtc:answer',{to:from,answer})});socket.on('webrtc:answer',async({from,answer})=>{const pc=peers.get(from);if(pc)await pc.setRemoteDescription(answer)});socket.on('webrtc:ice',async({from,candidate})=>{const pc=peers.get(from);if(pc)try{await pc.addIceCandidate(candidate)}catch(e){}});
  document.getElementById('clear')?.addEventListener('click',()=>{if(!(C.isTeacher||C.canWrite))return;const d=pages[currentPage]?.querySelector('.draw-layer');if(d)d.getContext('2d').clearRect(0,0,d.width,d.height);socket.emit('classroom:clear')});document.querySelectorAll('.right-tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.right-tab').forEach(t=>t.classList.remove('active'));document.querySelectorAll('.right-panel').forEach(p=>p.classList.remove('active'));tab.classList.add('active');document.getElementById('panel-'+tab.dataset.panel)?.classList.add('active')}));
  const chatMessages=document.getElementById('chatMessages');function addChat(m){if(!chatMessages)return;const d=document.createElement('div');d.className='chat-msg';d.innerHTML='<b></b><span></span>';d.querySelector('b').textContent=m.user||'Thành viên';d.querySelector('span').textContent=m.text;chatMessages.appendChild(d);chatMessages.scrollTop=chatMessages.scrollHeight}socket.on('classroom:chat',addChat);document.getElementById('chatForm')?.addEventListener('submit',e=>{e.preventDefault();const i=document.getElementById('chatInput');if(!i.value.trim())return;socket.emit('classroom:chat',{text:i.value.trim()});i.value=''});socket.on('classroom:presence',({count})=>{const el=document.getElementById('presenceText');if(el)el.textContent=`Đang có ${count} người trong phòng`});socket.emit('classroom:join',{room:C.room});if(!C.isTeacher)socket.emit('classroom:request-stream',{id:socket.id});const pdfButton=document.querySelector('[data-open-pdf]');if(pdfButton)loadPdf(pdfButton.dataset.openPdf).catch(()=>{});else{const p=makePage(0);pages=[p];pagesEl.appendChild(p);pageTotal.textContent='1'}
})();