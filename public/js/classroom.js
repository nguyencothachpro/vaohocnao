(() => {
  const C=window.CLASSROOM||{}, socket=io(), pagesEl=document.getElementById('pages'), viewport=document.getElementById('bookViewport'), emptyBoard=document.getElementById('emptyBoard'), zoomLabel=document.getElementById('zoomLabel'), pageNumber=document.getElementById('pageNumber'), pageTotal=document.getElementById('pageTotal');
  let mode='pen',currentPage=0,pages=[],pageFlip=null,pdf=null,zoom=1,localStream=null,processedStream=null,keyOn=false,keyTimer=null;const peers=new Map();
  function setMode(next){mode=next;document.querySelectorAll('.side-tool').forEach(b=>b.classList.remove('active'));const map={pen:'pen',highlighter:'highlighter',eraser:'eraser',text:'textTool'};if(map[next])document.getElementById(map[next])?.classList.add('active')}
  ['pen','highlighter','eraser'].forEach(id=>document.getElementById(id)?.addEventListener('click',()=>setMode(id)));document.getElementById('textTool')?.addEventListener('click',()=>setMode('text'));document.getElementById('noteTool')?.addEventListener('click',()=>openPanel('notes'));
  const openPanel=p=>{document.querySelectorAll('.right-tab').forEach(t=>t.classList.toggle('active',t.dataset.panel===p));document.querySelectorAll('.right-panel').forEach(x=>x.classList.toggle('active',x.id==='panel-'+p));document.getElementById('classroomRight')?.classList.add('mobile-open')};
  document.getElementById('openMaterials')?.addEventListener('click',()=>openPanel('materials'));document.getElementById('openNotes')?.addEventListener('click',()=>openPanel('notes'));document.getElementById('openChat')?.addEventListener('click',()=>openPanel('chat'));
  document.querySelectorAll('.right-tab').forEach(t=>t.addEventListener('click',()=>{document.getElementById('classroomRight')?.classList.remove('mobile-open');openPanel(t.dataset.panel)}));
  function updateZoom(){if(zoomLabel)zoomLabel.textContent=Math.round(zoom*100)+'%';if(pagesEl){pagesEl.style.transform=`scale(${zoom})`;pagesEl.style.marginBottom=Math.max(0,(zoom-1)*800)+'px'}}
  document.getElementById('zoomIn')?.addEventListener('click',()=>{zoom=Math.min(2.75,zoom+.1);updateZoom()});document.getElementById('zoomOut')?.addEventListener('click',()=>{zoom=Math.max(.5,zoom-.1);updateZoom()});document.getElementById('fit')?.addEventListener('click',()=>{zoom=1;updateZoom();viewport?.scrollTo({top:0,behavior:'smooth'})});document.getElementById('resetView')?.addEventListener('click',()=>{zoom=1;updateZoom();viewport?.scrollTo({top:0,behavior:'smooth'})});document.getElementById('fullscreen')?.addEventListener('click',async()=>{const t=document.querySelector('.classroom-app');if(!document.fullscreenElement)await t?.requestFullscreen?.();else await document.exitFullscreen?.()});
  function emitBoard(payload){socket.emit('classroom:board',{...payload,user:C.currentUser||'Thành viên'})}
  function makePage(index,pdfCanvas){const host=document.createElement('div');host.className='page-host';host.dataset.page=index;if(pdfCanvas){pdfCanvas.style.width='100%';pdfCanvas.style.height='100%';host.appendChild(pdfCanvas)}const draw=document.createElement('canvas');draw.className='draw-layer';host.appendChild(draw);let drawing=false,last=null;const resize=()=>{const r=host.getBoundingClientRect();if(!r.width||!r.height)return;const prev=draw.width&&draw.height?draw.toDataURL():null;draw.width=Math.round(r.width*devicePixelRatio);draw.height=Math.round(r.height*devicePixelRatio);draw.style.width=r.width+'px';draw.style.height=r.height+'px';const ctx=draw.getContext('2d');ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);if(prev){const img=new Image();img.onload=()=>ctx.drawImage(img,0,0,r.width,r.height);img.src=prev}};if(window.ResizeObserver)new ResizeObserver(resize).observe(host);requestAnimationFrame(resize);
    draw.addEventListener('pointerdown',e=>{if(mode==='text'){const text=prompt('Nhập nội dung cần viết:');if(!text)return;const ctx=draw.getContext('2d');ctx.save();ctx.fillStyle='#111827';ctx.font='20px Arial';ctx.fillText(text,e.offsetX,e.offsetY);ctx.restore();emitBoard({page:index,kind:'text',x:e.offsetX,y:e.offsetY,text});return}if(C.isTeacher===false&&document.getElementById('requestWrite')?.disabled===false&&!document.body.classList.contains('classroom-can-write'))return;draw.setPointerCapture(e.pointerId);drawing=true;last=[e.offsetX,e.offsetY]});
    draw.addEventListener('pointerup',()=>{drawing=false;last=null});draw.addEventListener('pointercancel',()=>{drawing=false;last=null});draw.addEventListener('pointermove',e=>{if(!drawing)return;const x=e.offsetX,y=e.offsetY,ctx=draw.getContext('2d'),erase=mode==='eraser';ctx.save();ctx.globalCompositeOperation=erase?'destination-out':'source-over';ctx.lineWidth=erase?26:(mode==='highlighter'?18:3);ctx.lineCap='round';ctx.lineJoin='round';ctx.strokeStyle=mode==='highlighter'?'rgba(250,204,21,.35)':'#ef4444';ctx.beginPath();ctx.moveTo(last[0],last[1]);ctx.lineTo(x,y);ctx.stroke();ctx.restore();emitBoard({page:index,kind:'stroke',x1:last[0],y1:last[1],x2:x,y2:y,erase,highlight:mode==='highlighter'});last=[x,y]});return host}
  function applyRemote(p){const host=pages[p.page],draw=host?.querySelector('.draw-layer');if(!draw)return;const r=host.getBoundingClientRect(),ctx=draw.getContext('2d'),sx=r.width/(draw.width/devicePixelRatio),sy=r.height/(draw.height/devicePixelRatio);ctx.save();ctx.globalCompositeOperation=p.erase?'destination-out':'source-over';ctx.lineCap='round';ctx.lineJoin='round';ctx.lineWidth=p.erase?26:(p.highlight?18:3);ctx.strokeStyle=p.highlight?'rgba(250,204,21,.35)':'#ef4444';if(p.kind==='stroke'){ctx.beginPath();ctx.moveTo(p.x1/sx,p.y1/sy);ctx.lineTo(p.x2/sx,p.y2/sy);ctx.stroke()}else if(p.kind==='text'){ctx.globalCompositeOperation='source-over';ctx.fillStyle='#111827';ctx.font='20px Arial';ctx.fillText(p.text,p.x/sx,p.y/sy)}ctx.restore()}
  socket.on('classroom:board',applyRemote);socket.on('classroom:clear',()=>pages.forEach(p=>p.querySelector('.draw-layer')?.getContext('2d').clearRect(0,0,p.querySelector('.draw-layer').width,p.querySelector('.draw-layer').height));
  function goToPage(n,broadcast=true){n=Math.max(0,Math.min((pages.length||1)-1,Number(n)||0));currentPage=n;if(pageFlip)try{pageFlip.flip(n)}catch(e){}if(pageNumber)pageNumber.value=n+1;if(broadcast&&C.isTeacher)socket.emit('classroom:page',{page:n})}
  document.getElementById('prev')?.addEventListener('click',()=>goToPage(currentPage-1));document.getElementById('next')?.addEventListener('click',()=>goToPage(currentPage+1));pageNumber?.addEventListener('change',()=>goToPage(Number(pageNumber.value)-1));socket.on('classroom:page',({page})=>{if(!C.isTeacher)goToPage(page,false)});
  function showPdfError(message){
    pagesEl.innerHTML=`<div class="pdf-error"><div class="pdf-error-icon">⚠️</div><h5>Không mở được tài liệu</h5><p>${message}</p><button type="button" id="retryPdf" class="btn btn-primary btn-sm">Thử mở lại</button></div>`;
    pageTotal.textContent='0';emptyBoard?.classList.add('d-none');
    document.getElementById('retryPdf')?.addEventListener('click',()=>loadPdf('/phong-hoc/'+C.room+'/pdf'));
  }
  async function loadPdf(url){
    if(!url) return;
    try{
      if(!window.pdfjsLib)throw new Error('PDF viewer chưa tải xong.');
      pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';
      const response=await fetch(url,{credentials:'same-origin',cache:'no-store'});
      if(!response.ok)throw new Error(`Máy chủ PDF trả về HTTP ${response.status}.`);
      const buffer=await response.arrayBuffer();
      if(buffer.byteLength<5)throw new Error('Máy chủ trả về tệp rỗng.');
      const signature=new TextDecoder().decode(new Uint8Array(buffer.slice(0,5)));
      if(!signature.startsWith('%PDF'))throw new Error('Nguồn tài liệu không trả về PDF hợp lệ. Hãy kiểm tra link PDF trong quản trị.');
      pdf=await pdfjsLib.getDocument({data:buffer}).promise;
      pagesEl.innerHTML='';pages=[];
      for(let i=1;i<=pdf.numPages;i++){
        const p=await pdf.getPage(i),vp=p.getViewport({scale:1.65}),c=document.createElement('canvas');
        c.width=vp.width;c.height=vp.height;
        await p.render({canvasContext:c.getContext('2d'),viewport:vp}).promise;
        pagesEl.appendChild(makePage(i-1,c));
      }
      pages=[...pagesEl.children];pageTotal.textContent=pages.length;emptyBoard?.classList.add('d-none');
      if(window.St?.PageFlip){
        pageFlip=new St.PageFlip(pagesEl,{width:760,height:1074,size:'stretch',minWidth:280,maxWidth:1200,minHeight:400,maxHeight:1700,showCover:true,maxShadowOpacity:.35,useMouseEvents:true,mobileScrollSupport:true});
        pageFlip.loadFromHTML(pages);
        pageFlip.on('flip',e=>{currentPage=e.data;pageNumber.value=currentPage+1;if(C.isTeacher)socket.emit('classroom:page',{page:currentPage})});
      }
      goToPage(0,false);updateZoom();
    }catch(e){console.error('Classroom PDF error:',e);showPdfError(e?.message||'Không xác định được lỗi khi mở PDF.');}
  }
  async function openMaterial(url,kind,broadcast=true){if(!url)return;if(kind==='pdf'||/\.pdf(?:\?|$)/i.test(url)){await loadPdf(url);if(broadcast&&C.isTeacher&&pages.length)socket.emit('classroom:material-open',{url,kind});return}if(kind==='image'||/\.(png|jpe?g|webp|gif)(?:\?|$)/i.test(url)){const img=new Image();img.crossOrigin='anonymous';img.onload=()=>{const c=document.createElement('canvas');c.width=img.naturalWidth;c.height=img.naturalHeight;c.getContext('2d').drawImage(img,0,0);pagesEl.innerHTML='';pages=[makePage(0,c)];pagesEl.appendChild(pages[0]);pageTotal.textContent='1';emptyBoard?.classList.add('d-none');if(C.isTeacher&&broadcast)socket.emit('classroom:material-open',{url,kind})};img.onerror=()=>alert('Không mở được ảnh.');img.src=url;return}window.open(url,'_blank','noopener')}
  document.querySelectorAll('[data-open-pdf]').forEach(b=>b.addEventListener('click',()=>openMaterial(b.dataset.openPdf,'pdf')));document.querySelectorAll('[data-material-url]').forEach(b=>b.addEventListener('click',()=>openMaterial(b.dataset.materialUrl,b.dataset.materialKind)));document.getElementById('openPdf')?.addEventListener('click',()=>C.initialPdf&&openMaterial('/phong-hoc/'+C.room+'/pdf','pdf'));socket.on('classroom:material-open',({url,kind})=>{if(!C.isTeacher)openMaterial(url,kind,false)});
  async function camera(){try{localStream=await navigator.mediaDevices.getUserMedia({video:true,audio:true});document.getElementById('localVideo').srcObject=localStream;document.getElementById('videoPip')?.classList.remove('d-none');if(C.isTeacher)socket.emit('classroom:teacher-stream')}catch(e){alert('Không mở được camera/micro. Hãy cấp quyền trình duyệt rồi thử lại.')}}document.getElementById('cam')?.addEventListener('click',camera);
  document.getElementById('screen')?.addEventListener('click',async()=>{if(!C.isTeacher){alert('Chỉ giáo viên mới chia sẻ màn hình cho cả phòng.');return}try{localStream=await navigator.mediaDevices.getDisplayMedia({video:true,audio:true});document.getElementById('localVideo').srcObject=localStream;document.getElementById('videoPip')?.classList.remove('d-none');socket.emit('classroom:teacher-stream')}catch(e){}});
  document.getElementById('green')?.addEventListener('click',()=>{if(!C.isTeacher){alert('Tính năng xóa nền chỉ dành cho giáo viên.');return}if(!localStream){alert('Bật Camera trước.');return}keyOn=!keyOn;if(keyOn)applyGreenKey();else{if(keyTimer)cancelAnimationFrame(keyTimer);processedStream=null;document.getElementById('localVideo').srcObject=localStream}});
  function applyGreenKey(){const v=document.getElementById('localVideo'),c=document.createElement('canvas'),ctx=c.getContext('2d');c.width=640;c.height=360;const tick=()=>{if(!keyOn)return;ctx.drawImage(v,0,0,c.width,c.height);const img=ctx.getImageData(0,0,c.width,c.height),d=img.data;for(let i=0;i<d.length;i+=4){const r=d[i],g=d[i+1],b=d[i+2];if(g>r*1.35&&g>b*1.15&&g>70)d[i+3]=0}ctx.putImageData(img,0,0);keyTimer=requestAnimationFrame(tick)};tick();processedStream=c.captureStream(30);localStream.getAudioTracks().forEach(t=>processedStream.addTrack(t));document.getElementById('localVideo').srcObject=processedStream;socket.emit('classroom:teacher-stream')}
  socket.on('classroom:request-stream',async({id})=>{if(!C.isTeacher||!(processedStream||localStream))return;const pc=new RTCPeerConnection();peers.set(id,pc);(processedStream||localStream).getTracks().forEach(t=>pc.addTrack(t,processedStream||localStream));pc.onicecandidate=e=>e.candidate&&socket.emit('webrtc:ice',{to:id,candidate:e.candidate});const offer=await pc.createOffer();await pc.setLocalDescription(offer);socket.emit('webrtc:offer',{to:id,offer})});socket.on('classroom:teacher-stream',()=>{if(!C.isTeacher)socket.emit('classroom:request-stream',{id:socket.id})});socket.on('webrtc:offer',async({from,offer})=>{if(C.isTeacher)return;const pc=new RTCPeerConnection();peers.set(from,pc);pc.ontrack=e=>{document.getElementById('remoteVideo').srcObject=e.streams[0];document.getElementById('remotePip')?.classList.remove('d-none')};pc.onicecandidate=e=>e.candidate&&socket.emit('webrtc:ice',{to:from,candidate:e.candidate});await pc.setRemoteDescription(offer);const answer=await pc.createAnswer();await pc.setLocalDescription(answer);socket.emit('webrtc:answer',{to:from,answer})});socket.on('webrtc:answer',async({from,answer})=>{const pc=peers.get(from);if(pc)await pc.setRemoteDescription(answer)});socket.on('webrtc:ice',async({from,candidate})=>{const pc=peers.get(from);if(pc)try{await pc.addIceCandidate(candidate)}catch(e){}});
  document.getElementById('clear')?.addEventListener('click',()=>{const d=pages[currentPage]?.querySelector('.draw-layer');if(d)d.getContext('2d').clearRect(0,0,d.width,d.height);if(C.isTeacher)socket.emit('classroom:clear')});
  const notes=document.getElementById('notesArea');if(notes){notes.value=localStorage.getItem('vaohocnao_notes_'+C.room)||'';let tm;notes.addEventListener('input',()=>{clearTimeout(tm);tm=setTimeout(()=>{localStorage.setItem('vaohocnao_notes_'+C.room,notes.value);document.getElementById('notesSaved').textContent='Đã tự lưu';},250)})}
  const chatMessages=document.getElementById('chatMessages');function addChat(m){if(!chatMessages)return;const d=document.createElement('div');d.className='chat-msg';d.innerHTML='<b></b><span></span>';d.querySelector('b').textContent=m.user||'Thành viên';d.querySelector('span').textContent=m.text||'';chatMessages.appendChild(d);chatMessages.scrollTop=chatMessages.scrollHeight}socket.on('classroom:chat',addChat);document.getElementById('chatForm')?.addEventListener('submit',e=>{e.preventDefault();const i=document.getElementById('chatInput');if(!i.value.trim())return;socket.emit('classroom:chat',{text:i.value.trim(),user:C.currentUser||'Thành viên'});i.value=''});socket.on('classroom:presence',({count})=>{const el=document.getElementById('presenceText');if(el)el.textContent=`${count} người trong phòng`});
  socket.emit('classroom:join',{room:C.room,role:C.isTeacher?'teacher':'student',user:C.currentUser});if(!C.isTeacher)socket.emit('classroom:request-stream',{id:socket.id});if(C.initialPdf)loadPdf('/phong-hoc/'+C.room+'/pdf');else{const p=makePage(0);pages=[p];pagesEl.appendChild(p);pageTotal.textContent='1'}
})();