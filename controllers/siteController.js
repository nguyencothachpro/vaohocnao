const Category = require('../models/Category');
const Course = require('../models/Course');
const Banner = require('../models/Banner');
const News = require('../models/News');
const Enrollment = require('../models/Enrollment');
const axios = require('axios');
const path = require('path');
const { resolveDirectDownloadUrl } = require('../utils');
exports.home = async (req,res)=>{const Book=require('../models/Book');const OnlineBook=require('../models/OnlineBook');const [categories,courses,books,onlineBooks,banners,news]=await Promise.all([Category.tree(),Course.published(),Book.published(),OnlineBook.published(),Banner.active(),News.published()]);res.render('index',{categories,courses:courses.slice(0,8),books:books.slice(0,4),onlineBooks:onlineBooks.slice(0,4),banners,news:news.slice(0,4)});};
exports.allCourses=async(req,res)=>{const courses=await Course.published();const categories=await Category.tree();res.render('all-courses',{courses,categories});};
exports.categoryPage=async(req,res)=>{const category=await Category.findBySlug(req.params.slug);if(!category)return res.status(404).render('404');const courses=await Course.byCategory(category.id);const categories=await Category.tree();res.render('category',{category,courses,categories});};
exports.courseDetail=async(req,res)=>{const course=await Course.findBySlug(req.params.slug);if(!course)return res.status(404).render('404');await Course.incrementView(course.id);const chapters=await Course.fullTree(course.id);let enrolled=false;if(req.session.user)enrolled=await Enrollment.isEnrolled(req.session.user.id,course.id);res.render('course-detail',{course,chapters,enrolled});};
exports.allBooks=async(req,res)=>{const Book=require('../models/Book'),BookType=require('../models/BookType'),BookCategory=require('../models/BookCategory');const categories=await BookCategory.tree(),bookTypes=await BookType.all();let category_ids=null,book_type_id=null;if(req.query.danh_muc){const cat=await BookCategory.findBySlug(req.query.danh_muc);if(cat)category_ids=await BookCategory.subtreeIds(cat.id)}if(req.query.the_loai){const bt=await BookType.findBySlug(req.query.the_loai);if(bt)book_type_id=bt.id}const books=await Book.published({category_ids,book_type_id});res.render('all-books',{books,categories,bookTypes,activeCategory:req.query.danh_muc||null,activeType:req.query.the_loai||null});};
exports.bookDetail=async(req,res)=>{const Book=require('../models/Book'),BookPurchase=require('../models/BookPurchase');const book=await Book.findBySlug(req.params.slug);if(!book)return res.status(404).render('404');let purchased=false;if(req.session.user)purchased=await BookPurchase.isPurchased(req.session.user.id,book.id);res.render('book-detail',{book,purchased});};
exports.onlineReadingBooks=async(req,res)=>{const OnlineBook=require('../models/OnlineBook'),OnlineBookCategory=require('../models/OnlineBookCategory');const categories=await OnlineBookCategory.tree();let category_ids=null;if(req.query.danh_muc){const cat=await OnlineBookCategory.findBySlug(req.query.danh_muc);if(cat)category_ids=await OnlineBookCategory.subtreeIds(cat.id)}const books=await OnlineBook.published({category_ids});res.render('online-reading-books',{books,categories,activeCategory:req.query.danh_muc||null});};
exports.onlineBookDetail=async(req,res)=>{const OnlineBook=require('../models/OnlineBook'),OnlineBookChapter=require('../models/OnlineBookChapter'),OnlineBookPurchase=require('../models/OnlineBookPurchase');const book=await OnlineBook.findBySlug(req.params.slug);if(!book)return res.status(404).render('404');const chapters=book.file_url?[]:await OnlineBookChapter.byBook(book.id);let purchased=false;if(req.session.user)purchased=await OnlineBookPurchase.isPurchased(req.session.user.id,book.id);res.render('online-book-detail',{book,chapters,purchased});};
exports.onlineBookRead=async(req,res)=>{const OnlineBook=require('../models/OnlineBook'),OnlineBookPurchase=require('../models/OnlineBookPurchase');const book=await OnlineBook.findBySlug(req.params.slug);if(!book)return res.status(404).render('404');let purchased=false;if(req.session.user)purchased=await OnlineBookPurchase.isPurchased(req.session.user.id,book.id);const unlocked=book.price===0||purchased;if(book.file_url){const OnlineBookTocEntry=require('../models/OnlineBookTocEntry');const manualToc=await OnlineBookTocEntry.byBook(book.id);return res.render('online-book-reader-v3',{book,mode:'pdf',unlocked,purchased,chapters:[],manualToc});}const OnlineBookChapter=require('../models/OnlineBookChapter');const chapters=await OnlineBookChapter.byBook(book.id);if(!chapters.length)return res.redirect(`/doc-sach-online/${book.slug}`);const unlockedChapters=chapters.map(c=>({...c,unlocked:book.price===0||c.is_free===1||purchased}));res.render('online-book-reader-v3',{book,mode:'chapters',chapters:unlockedChapters,purchased,unlocked,manualToc:[]});};
exports.onlineBookFile=async(req,res)=>{const OnlineBook=require('../models/OnlineBook'),OnlineBookPurchase=require('../models/OnlineBookPurchase');const book=await OnlineBook.findBySlug(req.params.slug);if(!book||!book.file_url)return res.status(404).end();let purchased=false;if(req.session.user)purchased=await OnlineBookPurchase.isPurchased(req.session.user.id,book.id);if(book.price>0&&!purchased)return res.status(403).end();try{if(book.file_url.startsWith('/uploads/'))return res.sendFile(path.join(__dirname,'..','public',book.file_url));const directUrl=resolveDirectDownloadUrl(book.file_url);const upstream=await axios.get(directUrl,{responseType:'stream',maxRedirects:5,headers:{'User-Agent':'Mozilla/5.0'}});res.setHeader('Content-Type','application/pdf');upstream.data.pipe(res);}catch(err){console.error('Loi tai file sach doc online:',err.message);res.status(502).send('Không tải được file sách. Hãy kiểm tra lại link chia sẻ hoặc upload lại PDF.');}};
exports.newsList=async(req,res)=>{const news=await News.published();res.render('news-list',{news});};
exports.newsDetail=async(req,res)=>{const article=await News.findBySlug(req.params.slug);if(!article)return res.status(404).render('404');res.render('news-detail',{article});};
